name: Build Android APK

on:
  push:
    branches: [ main ]
  pull_request:
    branches: [ main ]
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Set up JDK 17
        uses: actions/setup-java@v4
        with:
          java-version: '17'
          distribution: 'temurin'

      - name: Setup Gradle
        uses: gradle/actions/setup-gradle@v3

      - name: Make gradlew executable
        run: chmod +x gradlew

      - name: Create keystore directory
        run: mkdir -p app/keystore

      - name: Decode keystore
        run: echo "${{ secrets.KEYSTORE_BASE64 }}" | base64 -d > app/keystore/release.keystore
        if: secrets.KEYSTORE_BASE64 != ''

      - name: Build Debug APK
        run: ./gradlew assembleDebug

      - name: Build Release APK (Signed)
        if: secrets.KEYSTORE_BASE64 != ''
        run: ./gradlew assembleRelease
        env:
          KEYSTORE_PATH: app/keystore/release.keystore
          KEYSTORE_PASSWORD: ${{ secrets.KEYSTORE_PASSWORD }}
          KEY_ALIAS: ${{ secrets.KEY_ALIAS }}
          KEY_PASSWORD: ${{ secrets.KEY_PASSWORD }}

      - name: Upload Debug APK
        uses: actions/upload-artifact@v4
        with:
          name: hitif-videodownloader-debug
          path: app/build/outputs/apk/debug/*.apk

      - name: Upload Release APK
        if: secrets.KEYSTORE_BASE64 != ''
        uses: actions/upload-artifact@v4
        with:
          name: hitif-videodownloader-release
          path: app/build/outputs/apk/release/*.apk
