<?xml version="1.0" encoding="utf-8"?>
<LinearLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:orientation="vertical"
    android:padding="12dp"
    android:layout_marginStart="12dp"
    android:layout_marginEnd="12dp"
    android:layout_marginTop="4dp"
    android:background="@drawable/bg_card_item">

    <!-- Row 1: Info + Actions -->
    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="horizontal"
        android:gravity="center_vertical">

        <!-- Info column -->
        <LinearLayout android:layout_width="0dp" android:layout_height="wrap_content"
            android:layout_weight="1" android:orientation="vertical" android:layout_marginEnd="8dp">

            <TextView android:id="@+id/tvFilename"
                android:layout_width="match_parent" android:layout_height="wrap_content"
                android:textColor="@color/text_primary" android:textSize="12sp"
                android:fontFamily="@font/rajdhani_medium"
                android:maxLines="2" android:ellipsize="middle"/>

            <TextView android:id="@+id/tvSeries"
                android:layout_width="wrap_content" android:layout_height="wrap_content"
                android:textColor="@color/accent_violet" android:textSize="11sp"
                android:fontFamily="@font/rajdhani_medium"
                android:layout_marginTop="1dp" android:visibility="gone"/>

            <LinearLayout android:layout_width="match_parent" android:layout_height="wrap_content"
                android:orientation="horizontal" android:layout_marginTop="3dp"
                android:gravity="center_vertical">

                <TextView android:id="@+id/tvDate"
                    android:layout_width="0dp" android:layout_height="wrap_content"
                    android:layout_weight="1" android:textColor="@color/text_dim" android:textSize="10sp"
                    android:fontFamily="@font/rajdhani_medium"/>

                <TextView android:id="@+id/tvSize"
                    android:layout_width="wrap_content" android:layout_height="wrap_content"
                    android:textColor="@color/text_dim" android:textSize="10sp"
                    android:fontFamily="@font/rajdhani_medium"
                    android:layout_marginStart="8dp"/>

                <TextView android:id="@+id/tvSpeed"
                    android:layout_width="wrap_content" android:layout_height="wrap_content"
                    android:textColor="@color/accent_cyan" android:textSize="10sp"
                    android:fontFamily="@font/rajdhani_medium"
                    android:layout_marginStart="8dp"
                    android:visibility="gone"/>

                <TextView android:id="@+id/tvState"
                    android:layout_width="wrap_content" android:layout_height="wrap_content"
                    android:textSize="10sp" android:fontFamily="@font/rajdhani_medium" android:layout_marginStart="8dp" android:letterSpacing="0.04"/>
            </LinearLayout>
        </LinearLayout>

        <!-- Actions -->
        <ImageButton android:id="@+id/btnShare"
            android:layout_width="28dp" android:layout_height="28dp"
            android:background="@drawable/bg_icon_btn" android:src="@drawable/ic_share"
            android:padding="6dp" android:contentDescription="Partager"/>

        <ImageButton android:id="@+id/btnRetry"
            android:layout_width="28dp" android:layout_height="28dp" android:layout_marginStart="4dp"
            android:background="@drawable/bg_icon_btn" android:src="@drawable/ic_refresh"
            android:padding="6dp" android:contentDescription="Relancer" android:visibility="gone"/>

        <ImageButton android:id="@+id/btnDelete"
            android:layout_width="28dp" android:layout_height="28dp" android:layout_marginStart="4dp"
            android:background="@drawable/bg_icon_btn" android:src="@drawable/ic_trash"
            android:padding="6dp" android:contentDescription="Supprimer"/>
    </LinearLayout>

    <!-- Progress bar (visible only during download) -->
    <ProgressBar
        android:id="@+id/progressBar"
        style="?android:attr/progressBarStyleHorizontal"
        android:layout_width="match_parent"
        android:layout_height="4dp"
        android:layout_marginTop="8dp"
        android:progressTint="@color/accent_cyan"
        android:progressBackgroundTint="@color/bg_surface"
        android:max="100"
        android:visibility="gone"/>

</LinearLayout>
