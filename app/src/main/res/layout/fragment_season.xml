<?xml version="1.0" encoding="utf-8"?>
<LinearLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:orientation="vertical"
    android:background="@drawable/bg_glass_bottom_sheet">

    <View android:layout_width="40dp" android:layout_height="4dp"
        android:layout_gravity="center_horizontal" android:layout_marginTop="10dp"
        android:layout_marginBottom="6dp" android:background="@drawable/bg_drag_handle"/>

    <!-- Header -->
    <LinearLayout android:layout_width="match_parent" android:layout_height="60dp"
        android:orientation="horizontal" android:paddingStart="16dp" android:paddingEnd="8dp"
        android:gravity="center_vertical" android:background="@drawable/bg_glass_panel_header">

        <LinearLayout android:layout_width="0dp" android:layout_height="wrap_content"
            android:layout_weight="1" android:orientation="vertical">

            <TextView android:id="@+id/tvSeriesName"
                android:layout_width="wrap_content" android:layout_height="wrap_content"
                android:text="SÉRIE" android:textColor="@color/accent_cyan"
                android:textSize="13sp" android:letterSpacing="0.1"
               />

            <TextView android:id="@+id/tvSeasonLabel"
                android:layout_width="wrap_content" android:layout_height="wrap_content"
                android:text="SAISON" android:textColor="@color/text_dim"
                android:textSize="11sp" android:letterSpacing="0.08"
               />
        </LinearLayout>

        <android.widget.Button android:id="@+id/btnSelectAll"
            android:layout_width="wrap_content" android:layout_height="30dp"
            android:text="TOUT" android:textColor="@color/accent_cyan"
            android:textSize="9sp" android:background="@drawable/bg_icon_btn"
            android:paddingStart="8dp" android:paddingEnd="8dp" android:layout_marginEnd="4dp"/>

        <android.widget.Button android:id="@+id/btnDeselectAll"
            android:layout_width="wrap_content" android:layout_height="30dp"
            android:text="AUCUN" android:textColor="@color/text_dim"
            android:textSize="9sp" android:background="@drawable/bg_icon_btn"
            android:paddingStart="8dp" android:paddingEnd="8dp" android:layout_marginEnd="4dp"/>

        <ImageButton android:id="@+id/btnClose"
            android:layout_width="36dp" android:layout_height="36dp"
            android:background="@drawable/bg_icon_btn" android:src="@drawable/ic_close"
            android:padding="9dp" android:contentDescription="Fermer"/>
    </LinearLayout>

    <View android:layout_width="match_parent" android:layout_height="1dp"
        android:background="@drawable/bg_divider_cyan"/>

    <!-- Episode list -->
    <androidx.recyclerview.widget.RecyclerView
        android:id="@+id/recyclerView"
        android:layout_width="match_parent" android:layout_height="0dp" android:layout_weight="1"
        android:clipToPadding="false" android:paddingTop="4dp" android:paddingBottom="4dp"/>

    <!-- Download button -->
    <LinearLayout android:layout_width="match_parent" android:layout_height="wrap_content"
        android:orientation="horizontal" android:padding="12dp"
        android:background="@drawable/bg_glass_panel_header">

        <Button android:id="@+id/btnDownloadSelected"
            android:layout_width="match_parent" android:layout_height="44dp"
            android:text="⬇  TÉLÉCHARGER LA SÉLECTION"
            android:textColor="@color/bg_primary"
            android:textSize="12sp" android:letterSpacing="0.08"
            android:backgroundTint="@color/accent_cyan"/>
    </LinearLayout>

</LinearLayout>
